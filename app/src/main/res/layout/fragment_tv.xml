<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/tv_fragment_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/dark_background"
    tools:context=".ui.tv.TvFragment">

    <!-- Barra de herramientas superior -->
    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/app_bar_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:background="@color/dark_surface">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar_tv"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            app:title="TV en Vivo"
            app:titleTextColor="@color/light_text">

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="end"
                android:orientation="horizontal">

                <ImageButton
                    android:id="@+id/btn_search_tv"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="Buscar"
                    android:src="@drawable/ic_search"
                    app:tint="@color/light_text" />

                <ImageButton
                    android:id="@+id/btn_settings_tv"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="Ajustes"
                    android:src="@drawable/ic_settings"
                    app:tint="@color/light_text" />
            </LinearLayout>
        </androidx.appcompat.widget.Toolbar>
    </com.google.android.material.appbar.AppBarLayout>

    <!-- Contenedor fijo para el reproductor y el título -->
    <LinearLayout
        android:id="@+id/player_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/app_bar_layout"
        android:orientation="vertical"
        android:background="@color/black">

        <androidx.media3.ui.PlayerView
            android:id="@+id/player_view_tv"
            android:layout_width="match_parent"
            android:layout_height="220dp"
            app:resize_mode="fit"
            app:use_controller="true"
            app:controller_layout_id="@layout/custom_player_controls"
            android:background="#000000"/>

        <TextView
            android:id="@+id/tv_now_playing_channel"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="12dp"
            android:textColor="@color/light_text"
            android:textSize="16sp"
            android:textStyle="bold"
            android:background="@color/dark_surface"
            android:visibility="gone"
            tools:visibility="visible"
            tools:text="Reproduciendo: Nombre del Canal"/>
    </LinearLayout>

    <!-- Lista de canales -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view_tv"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/player_container"
        android:padding="8dp"
        tools:listitem="@layout/item_tv_category" />

    <!-- CORREGIDO: Contenido de empty_state_layout.xml incrustado directamente -->
    <LinearLayout
        android:id="@+id/empty_state_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:layout_below="@id/player_container"
        android:gravity="center"
        android:visibility="gone">

        <ImageView
            android:id="@+id/iv_empty_icon"
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:src="@drawable/ic_no_internet"
            app:tint="@color/dark_text" />

        <TextView
            android:id="@+id/tv_empty_message"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Error al cargar el contenido."
            android:textColor="@color/dark_text"
            android:textSize="16sp"
            android:layout_marginTop="16dp"/>

        <Button
            android:id="@+id/btn_retry"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="Reintentar"
            android:visibility="gone"/>
    </LinearLayout>

    <ProgressBar
        android:id="@+id/progress_bar_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:visibility="gone" />

</RelativeLayout>
